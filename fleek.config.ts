import { FleekConfig } from '@fleekxyz/cli';

export default {
  "sites": [
    {
      "slug": "prehistoric-processor-few",
      "distDir": "out",
      "buildCommand": "npm run build"
    }
  ]
} satisfies FleekConfig;